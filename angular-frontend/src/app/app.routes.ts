import { Routes } from '@angular/router';
import { FileManagerComponent } from './file-manager/file-manager.component';

export const routes: Routes = [ { path: 'files', component: FileManagerComponent }];
